<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animeimages">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimeImages"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".SearchWallpaper"
            android:exported="false"
            android:label="@string/title_activity_search_wallpaper"
            android:theme="@style/Theme.AnimeImages.NoActionBar" />
        <activity
            android:name=".ImageViewText"
            android:exported="false"
            android:label="@string/title_activity_image_view_text"
            android:theme="@style/Theme.AnimeImages.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AnimeImages.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>